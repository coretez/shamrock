# DEBT — Tech-Debt Ledger

> Findings the bounded fix cycles left behind, appended automatically by the
> pipeline (O27) — nothing a review or check surfaces is allowed to evaporate.
> A finding recorded twice is flagged PROMOTE TO GATE: encode it as a check
> command, lint, or guard instead of prose. Tick entries off as they're fixed.

## Open findings

<!-- Closing note (glass-box entry): the first fix reported only rejections,
     which left silence ambiguous and produced a second unsupported claim. The
     event now carries proposed/kept/dropped, so no event means exactly one
     thing. Generalized into O17 as a standing requirement for every guard.
     VERIFIED 2026-08-14 by isolated A/B (old permissive wording + new filter,
     both in copies): the planner proposed 3 task parameters — output_target,
     format, scope — and the vocabulary filter rejected all 3; SPEC stayed
     clean. Both layers are load-bearing and were confirmed independently:
     the wording suppresses proposals (runs 2-3 proposed nothing), the filter
     catches what the wording does not. The junk keys differ every run, which
     is why this is an allowlist and could not have been a denylist. -->

- [x] 2026-08-14 · glass-box/high · src/main/plan-derive.js — O8 record rejection is invisible — normalizeRecords discards a model-proposed decision with no process event — fix: return the dropped records and emit a process event from ipc.js; O14 forbids invisible context engineering, and without it a working guard cannot be distinguished from a planner that never proposed anything _(observed 2026-08-13 while verifying the O8 fix)_ <!-- key:src/main/plan-derive.js|o8 record rejection is invisible — normalizerecords discards -->
- [x] 2026-08-14 · efficiency/high · src/main/ipc.js — plan_refines is recorded but nothing watches it — a replanning turn is the observable signature of a runaway cost turn — fix: compare the turn's measured input tokens against the project's recent median and emit a cost-outlier process event carrying the replan count _(run 1 of the diagram prompt: 3-step plan grew to 8 steps, 1.07M input tokens, 12.4 min)_ <!-- key:src/main/ipc.js|plan_refines is recorded but nothing watches it — a replanni -->
- [x] 2026-08-14 · drift/med · src/main/drift.js — O30 does not inspect diagrams inside canonical docs, so the exact rot that motivated it (a stale mermaid flowchart contradicting the code) is invisible to the drift pass — fix: extract fenced mermaid blocks from the canonical docs and include them in the scan set, filed against the doc name _(the stale PIPELINE_PSEUDOCODE.md flowchart was found by hand, not by O30)_ <!-- key:src/main/drift.js|o30 does not inspect diagrams inside canonical docs, so the  -->
- [ ] 2026-08-14 · docs/high · src/main/project-docs.js — the canonical docs must be MAINTAINED AND USED, not decoration. Superseded framing: an earlier entry proposed a "documentation accuracy KPI" derived from a token-cost observation that later proved to be noise — that metric was invented and is withdrawn. The real requirement: docs are read before planning (O15, shipped), written back after change (doc-writer, shipped), ORGANISED so they can be found and used (O31 librarian covers the library; the canonical set is not yet part of that), and kept honest (O30 drift, shipped). — fix: close the loop between the librarian and the canonical set, and add a use-signal — which docs actually reached a plan — so a doc nobody reads is visible as such _(open — the gap is organisation and use, not a number)_ <!-- key:src/main/project-docs.js|the canonical docs must be maintained and used, not decor -->
- [x] 2026-08-14 · design/high · src/main/ipc.js — a planner-INFERRED record is stored at user confidence with source "align", but nothing verifies the user ratified anything — O8 says "ratified user decisions", and an inference is being labelled as user testimony at the highest, overwrite-protected tier — fix: store an inferred record at derived confidence; reserve user confidence for values that came back through an actual align ratification, where the user really did answer _(open — false positives at user confidence are STICKY, they cannot be corrected by later observation)_ <!-- key:src/main/ipc.js|a planner-inferred record is stored at user confidence with  -->
- [x] 2026-08-14 · design/med · src/main/plan-derive.js — the record field is offered on EVERY turn and is not mode-gated, unlike its sibling decisions — fix: DECIDED 2026-08-14, do NOT gate it. Decisions must be captured wherever the user states them, including plain chat; capture is silent and passive and must never become another thing the user has to service. The false-positive risk it was raised for is handled by the durable-key vocabulary plus derived-confidence tiering, neither of which costs the user an interaction. The design rule this settles: CAPTURE IS PASSIVE — only the O7 align gate may stop a turn, and only when direction is genuinely undecided _(closed by decision, not by code)_ <!-- key:src/main/plan-derive.js|the record field is offered on every turn and is not mode-ga -->
- [x] 2026-08-14 · robustness/high · src/main/execute.js — a connector idle-timeout abort mid-step KILLS the whole turn — executeStep re-throws because stopped() only tests the USER abort, so a provider that goes quiet past the 300s idle window loses every completed step, the synthesis, and the persistence, contradicting O12 ("every failure lands somewhere safer") — fix: distinguish a connector abort from a user STOP and degrade it to the STUCK path (partial conclusion, then refine or escalate) so completed work still reaches synthesis and the store _(observed 2026-08-14: run 4 of the diagram prompt died at step 2 with AbortError from openai-compat.js idle timer; all step-1 work lost)_ <!-- key:src/main/execute.js|a connector idle-timeout abort mid-step kills the whole turn -->
- [x] 2026-08-14 · glass-box/med · src/main/execute.js — a REFUSED set_variable was reported as a var-set event with key:null, so the process rail showed captures that never happened — fix: emit var-rejected when the store refuses the write and var-set only when it accepts, carrying the confidence tier _(FIXED 2026-08-14 — observed 5 phantom captures in one turn; smoke-covered both directions)_ <!-- key:src/main/execute.js|a refused set_variable was reported as a var-set event with  -->
- [x] 2026-08-14 · correctness/high · src/main/execute.js — a re-plan triggered by a PROVIDER STALL replaced the whole remaining tail and deleted the steps that produced the deliverable — the turn then reported completion having written nothing — fix: a transport failure now retries the same step (TRANSPORT_RETRIES=2) and never re-plans, because a stalled connection says nothing about the plan; genuine task-level stuck still re-plans _(FIXED 2026-08-14 — observed live: 4-step plan, step 2 stalled twice, steps 3-4 vanished, git showed no step-commits; smoke pins that the deliverable step still runs)_ <!-- key:src/main/execute.js|a re-plan triggered by a provider stall replaced the whole r -->
- [x] 2026-08-14 · verification/high · docs/PHASE_PLAN.md — nothing checks that a plan produced the artifact it declared — a turn can complete, synthesize, and report success while its stated goal (write file X) went unmet; the per-step "produces" contract is never verified — fix: O26 does this for code by running the tests; there is no equivalent for "did this plan deliver what it said". Consider a deterministic goal check: a plan whose steps declared a file artifact but whose turn mutated nothing should surface as incomplete, not successful _(OPEN — root cause of the observed silent no-op was the replan bug (now fixed), but the missing verification is what let it report success)_ <!-- key:docs/PHASE_PLAN.md|nothing checks that a plan produced the artifact it declared -->
- [x] 2026-08-14 · documentation/med · docs/PIPELINE_PSEUDOCODE.md — semantic detail carried in mermaid <br/> SUB-LABELS is lost every time a model redraws the diagram — measured 8 of 8 redraws dropped the O26 baseline, "never re-inserts", "final O26 check as 1st finding" and "PROMOTE TO GATE", while additive sub-labels like "+ rulebook + check command" survived — fix: anything that must reach a reader is a NODE, not a sub-label. Baseline and the cannot-spiral property promoted to nodes; a drawing note above the diagram tells future redraws to shorten labels but never delete nodes _(FIXED 2026-08-14 for the two load-bearing cases; O11/O27 sub-labels left as-is and will be re-checked on the next redraw)_ <!-- key:docs/PIPELINE_PSEUDOCODE.md|semantic detail carried in mermaid br/ sub-labels is lost  -->
- [x] 2026-08-14 · method/high · docs/PIPELINE_PSEUDOCODE.md — an 8-of-8 miss streak was read as a systematic mechanism ("sub-labels are dropped, nodes survive") and acted on; a 14-trial controlled eval showed property survival is ~1-in-5 and uncorrelated with encoding, node budget, or explicit must-keep instructions — at p≈0.2 an 8-run miss streak occurs ~17% of the time, so the streak was ordinary variance — fix: a false empirical claim was corrected in the diagram drawing note. Standing rule: before explaining a streak with a mechanism, compute how likely the streak is under noise; and never write a measured claim into the docs from an uncontrolled sample _(FIXED 2026-08-14 (note corrected) — the underlying lesson is a process rule, not code)_ <!-- key:docs/PIPELINE_PSEUDOCODE.md|an 8-of-8 miss streak was read as a systematic mechanism ("s -->
- [x] 2026-08-14 · method/med · scripts/smoke.js — model-behaviour questions were being answered with 5-50 minute full agentic runs, when the question was a single model call — three theories were refuted in ~15 minutes once isolated as a focused eval, which would have cost roughly ten hours of harness runs — fix: keep a focused-eval harness alongside the smoke suite: one model call, N variants, a deterministic pass/fail predicate, replicates per arm. Reach for it whenever the question is about what the MODEL does rather than what the CODE does _(FIXED 2026-08-14 — promoted to scripts/eval.js + scripts/evals/redraw.js, wired to `npm run eval`, excluded from `npm test` since it costs API calls; replicates-per-arm is enforced by the runner rather than left to discipline)_ <!-- key:scripts/smoke.js|model-behaviour questions were being answered with 5-50 minu -->
